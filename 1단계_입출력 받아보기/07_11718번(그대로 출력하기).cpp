#include <bits/stdc++.h>
using namespace std;

int main()
{
	char c;
	while((c = getchar()) != EOF)
	{
		putchar(c);
	}
}
/* 
EOF = End of File
getchar() : 문자 입력 
putchar() : 문자 출력
즉, getchar()함수에서 차례로 받아 바로 putchar함수로 출력해준다. 
printf 와 scanf를 사용하면 시간 초과가 된다.  
*/ 
